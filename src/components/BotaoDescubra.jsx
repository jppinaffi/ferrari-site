export default function BotaoDescubra({ onClick }) {
  return (
    <button
      type="button"
      className="btn btn-outline-light"
      id="botaovideo"
      onClick={onClick}
    >
      Descubra
    </button>
  );
}
